<h1>Пройти тест: <%= @test_passage.test.title %></h1>

<b>
  <%= @test_passage.current_question.body %>
</b>

<p>
  Вопрос <%= @test_passage.current_question_id %> из <%= @test_passage.test.questions.count %>.
</p>

<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :title, include_hidden: false do |b| %>
    <div>
      <%= b.check_box id: "answer_ids_#{b.object.id}" %>
      <%= b.label %>
    </div>
  <% end %>

  <div>
    <%= form.submit 'Следующий вопрос' %>
  </div>
<% end %>